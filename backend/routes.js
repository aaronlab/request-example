const ROOT = "/";
const USER = "/user";
const JOIN = "/join";
const LOGIN = "/login";
const USER_DEATIL = "/:id";

const routes = {
  root: ROOT,
  user: USER,
  join: JOIN,
  login: LOGIN,
  userDetail: USER_DEATIL,
};

export default routes;
